# Admit contract — TypeSafe / Jev vs geometric-mean gate

Product: a-11-oy.com · proof: a11oy.net · never a11oy.com.
Doctrine v11 LOCKED. Λ = Conjecture 1. Trust ceiling 0.97 is policy.

This file is the pin for models, routers, kernels, and controllers.
Weights do not own promotion. A card, like-count, or HTTP 200 does not either.

## Split (do not collapse)

| Layer | Owns | Must not |
|---|---|---|
| TypeSafe Jev (`jev-latest`) | Choice / Noul / Score over supplied state | ALLOW, LIVE, AGI, ALL_DONE, theorem-close Λ |
| TypeSafe composite scoring | Weighted **sum** of dimension scores in code | Act as the admit function |
| Citation-check cookbook | `supports` / `contradicts` / `says_nothing` | Invent evidence |
| Public `/verify` + `szl_verify_nine` | Weighted **geometric mean** admit | Treat AM as a pass |
| Models (Khipu / Forge / routers) | Plans, abstention, cited handles | Self-admit, self-promote, mint LIVE |

Jev question lattice for a claim:

- `claim_kind` Choice: MEASURED | DREAMED | FORBIDDEN_PROMOTION
- `card_relation` Choice: supports | contradicts | says_nothing
- `promote_ok` Noul — code still denies promotion
- `evidence_strength` Score

Missing `TYPESAFE_API_KEY` is `UNAVAILABLE`. Fallback answers are SOFTWARE, not Jev.

## Locked math (equal-weight n=4, POLICY_TAU = 0.80)

G = exp(Σ log xᵢ / n). Admit iff G ≥ 0.80 ∧ min > 0.
dang ≔ (AM ≥ τ) ∧ (GM < τ).

| vector | axes | GM | AM | min | admit | dang |
|---|---|---|---|---|---|---|
| nominal | 0.95, 0.92, 0.88, 0.90 | 0.9121 | 0.9125 | 0.8800 | true | false |
| provenance_drop | 0.95, 0.04, 0.88, 0.90 | 0.4165 | 0.6925 | 0.0400 | false | false |
| liar @0.80 | 0.97, 0.97, 0.97, 0.02 | 0.3676 | 0.7325 | 0.0200 | false | false |
| hidden_weak | 0.95, 0.95, 0.95, 0.40 | 0.7653 | 0.8125 | 0.4000 | false | true |
| false_friend | 0.95, 0.92, 0.90, 0.55 | 0.8110 | 0.8300 | 0.5500 | true | false |

liar is dang only at FRONTIER_DEFAULT_TAU = 0.70.
hidden_weak is the compensating-score demo. Arithmetic would hide it. GM does not.

## Labels

VERIFIED · MISMATCH · UNSIGNED-LOCAL · UNAVAILABLE · BLOCKED · ADMITTED · CONJECTURE · MEASURED

- ADMITTED is software, never a green VERIFIED pill.
- Energy UNAVAILABLE is not 0.0 J. joules=0 + UNAVAILABLE is MISMATCH.
- Optional Rekor / SCITT / C2PA / OTS / OMS / ZK stay UNAVAILABLE without a bundle.
- Optional UNAVAILABLE must not flip a crypto PASS to PARTIAL.

## Model instill

Pin `szl.model_admit_contract/v1` from `szl_verify_nine.model_contract()` on:

- controller / router system cards
- training and eval receipts (as declared policy, not as a theorem)
- HF model cards as a citation of this file + `/verify`
- Alloy / Hatun / a11oy organs that score axes

A model may propose axes. A model may abstain. A model may not ALLOW itself.
